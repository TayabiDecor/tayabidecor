## Packages
framer-motion | Page transitions, scroll-triggered animations, and Apple-like interactions
react-hook-form | Form state management for the contact form
@hookform/resolvers | Zod validation resolver for the contact form

## Notes
The application relies on `@shared/routes` and `@shared/schema` for Zod schema validation.
Using Playfair Display and Montserrat for the luxurious typography pairing.
Stock images are handled via Unsplash URLs as requested for visual placeholders.
